# --- Essential Secrets (Required) ---
# 24-word mnemonic (space separated)
MNEMONIC=word1 word2 ... word24

# Jupiter API Key (https://jup.ag)
JUP_API_KEY=

# --- Multi-Wallet Configuration (Optional) ---
# For running multiple wallets in parallel (npm run start -- multi-drip)
# Add as many as needed: WALLET_1_MNEMONIC, WALLET_2_MNEMONIC, ...
# WARNING: NEVER COMMIT REAL MNEMONICS TO GIT!
# WALLET_1_MNEMONIC=word1 word2 ... word24
# WALLET_2_MNEMONIC=word1 word2 ... word24

# --- Network Configuration ---
# Solana RPC URL (Use a private RPC for better reliability)
RPC_URL=https://api.mainnet-beta.solana.com

# HTTP Proxy (Optional, leave empty if not used)
# e.g. http://127.0.0.1:7890
PROXY_URL=

# --- Telegram Notifications ---
TG_BOT_TOKEN=
TG_CHAT_ID=
TG_ENABLED=true
TG_TIMEOUT_MS=10000
TG_MAX_RETRY=3
# Send notification for single swaps? (default: false)
TG_SINGLE_SWAP=false

# --- Logging & Operations ---
# Log level: info | debug
LOG_LEVEL=info

# --- DRIP Strategy Configuration ---
# Number of trades to execute in one run
DRIP_TRADES=150
# Total duration of the run in seconds (e.g., 3600 = 1 hour)
DRIP_WINDOW_SEC=3600
# Min/Max USDC value per trade
DRIP_USDC_MIN=2
DRIP_USDC_MAX=3
# Jitter percentage for interval randomization (0.35 = +/- 35%)
DRIP_JITTER_PCT=0.35
# Minimum delay between trades in seconds
DRIP_MIN_DELAY_SEC=5
# Backoff time in seconds after a failure
DRIP_FAIL_BACKOFF_SEC=20
# Starting direction: SOL_TO_USDC or USDC_TO_SOL
DRIP_START_DIRECTION=SOL_TO_USDC
# Skip trade if balance is insufficient (true/false)
DRIP_SKIP_IF_INSUFFICIENT=true
# Dry run mode (true = no transactions sent)
DRIP_DRY_RUN=false
